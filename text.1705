#include <stdio.h>
int main() 
{ 
int N;
scanf("%d",&N);
int a[10];
int b[10];
int i=1;
for(i=1;i<=N;i++)
{
scanf("%d %d",&a[i],&b[i]);	
}
int m=0;
int n=0;
int s=0;
for(i=1;i<=N;i++)
{
m=m+a[i];
}
for(i=1;i<=N;i++)
{
n=n+b[i];
}
s=60*m+n;
printf("%d",s);
}
